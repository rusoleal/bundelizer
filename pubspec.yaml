name: bundelizer
description: Flutter plugin to store/retrieve compressed data in a structured way.
version: 0.0.8
repository: https://github.com/rusoleal/bundelizer

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  archive: ^3.4.10
  uuid: ^4.3.2

dev_dependencies:
  flutter_lints: ^3.0.1
  test: ^1.24.6

topics:
  - persistence
  - storage